general {
    lock_cmd = pidoff hyprlock || hyprlock --grace 5    # dbus/sysd lock command (loginctl lock-session)
    unlock_cmd = killall hyprlock                   	# same as above, but unlock
    before_sleep_cmd = loginctl lock-session        	# command ran before sleep
    after_sleep_cmd = hyprctl dispatch dpms on     	    # command ran after sleep
    ignore_dbus_inhibit = false                     	# whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
    ignore_systemd_inhibit = false                  	# whether to ignore systemd-inhibit --what=idle inhibitors
}

# turn off screen faster if session is already locked
# listener {
#     timeout = 300                            # 30 seconds
#     on-timeout = pidof hyprlock && hyprctl dispatch dpms off # turns off the screen if hyprlock is active
#     on-resume = pidof hyprlock && hyprctl dispatch dpms on    # command to run when activity is detected after timeout has fired.
# }

# Warn
listener {
    timeout = 270                           # in seconds
    on-timeout = notify-send "You are idle!" # command to run when timeout has passed
    #on-resume = notify-send "Welcome back!"  # command to run when activity is detected after timeout has fired.
}

# Screenlock
listener {
    #timeout = 600                     # 10 min
    timeout = 300                     # 5 min
    on-timeout = loginctl lock-session # command to run when timeout has passed
    # on-resume = notify-send "Welcome back to your desktop!"  # command to run when activity is detected after timeout has fired.
}

#listener {
#    timeout = 1800                                # 30min
#    on-timeout = systemctl suspend                # suspend pc
#}